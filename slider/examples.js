

// let myFirstFunction = (name, greeting) => { //w () podajemy atrybut funkcji
//     console.log(greeting+  ' ' + name);
// };

// myFirstFunction('zaba', 'zielona'); //wywołanie funkcji w () mozna podac wartosc atrybutu funkcji
// myFirstFunction('słon', 'niebieski');

//w operatorach porownania < > wieksze mniejsze, === porownanie != nierowne 

let isBiggerThan10 = (number) => {
    if(number > 10) {
        console.log('liczba jest wieksza od 10');
    } else {
        console.log('liczba nie jest wieksza od 10');
    }
};

isBiggerThan10(2);